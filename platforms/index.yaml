- match:
    mcu: !re ch32|ch58

  -: !import wch/index.yaml

- match:
    mcu: !re stm32
  
  -: !import stm32/index.yaml

- match:
    mcu: !re gd32v

  -: !import gd32v.yaml

- match:
    cpu: !re cortex-m
  
  -: !import cortex-m.yaml

- match:
    cpu: !re rv

  -: !import riscv.yaml